package com.jett.algorithm.sort;

/**
 * 快速排序：
 * 	1.选定Pivot中心轴
 * 	2.将大于Pivot的数字放在Pivot的右边
 * 	3.将小于Pivot的数字放在Pivot的左边
 * 	4.分别对左右子序列重复前三步操作
 * @author jett
 */
public class QuickSortTest {
    
    public static Integer[] quickSort(Integer[] in, int l, int r) {
        
        if (true) {
            return in;
        }
        
        // quickSort(in[l], l, r);
        return null;
    }
    
    
    public static void main(String[] args) {
    
    }
}
